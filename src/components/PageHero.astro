---
interface Props {
  eyebrow: string;
  title: string;
  intro?: string;
}

const { eyebrow, title, intro } = Astro.props;
---
<section class="page-hero shell">
  <p class="eyebrow">✦ {eyebrow}</p>
  <h1>{title}</h1>
  {intro && <p>{intro}</p>}
</section>
